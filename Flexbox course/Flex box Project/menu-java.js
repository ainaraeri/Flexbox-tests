const button = document.getElementById("logo");
const nav = document.getElementById("links-wrapper", "link3-wrapper");

button.addEventListener('click', () => {
  links-wrapper.classList.toggle('show');
});